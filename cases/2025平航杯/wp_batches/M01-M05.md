# 2025 平航杯 — M 板块批次 1（M01-M05）

> 产出时间：2026-05-04
> 检材：`F:\TEXT(important)\Jian cai\2025平航杯\20250415_181118.zip`
> 模式：`training` + `exam_mode` 严格模式
> 工具：unzip / tar / sqlite3 / jadx / aapt / md5sum / python3+pycryptodome

---

## Q M01 — 备份提取时间

> 该检材的备份提取时间（UTC）（格式：2020/1/1 01:01:01）

**答案**：`2025/4/15 10:11:18` （置信度 高）

**解析**

- **识别**：检材文件名 `20250415_181118.zip` 是 MIUI 备份命名约定 `YYYYMMDD_HHmmss`（北京时间）。
- **提取**：zip 内 `descript.xml` 也含 time 字段佐证。
- **分析**：CST 18:11:18 → UTC -8h = **10:11:18**。
- **验证**：与 zip 内文件 mtime 一致。

**不作弊声明**：仅访问检材本地文件；工具 `ls` / `unzip`；脚本：无（直接观察文件名）。

---

## Q M02 — Puzzle_Game Flag1

> 分析倩倩的手机检材，手机内 Puzzle_Game 拼图程序拼图 APK 中的 Flag1 是什么（格式：xxxxxxxxx）

**答案**：`Key_1n_the_P1c` （置信度 高）

**解析**

- **识别**：浏览器下载目录中的 `Puzzle_Game.apk`（com.example.puzzlegame）。
- **提取**：jadx 反编译；定位 `com.example.puzzlegame.util.AESUtil`：
  - `MAGIC_NUMBERS XOR 0x06` → key = `weZl_d0wn_sbwyz_`
  - 拼接的 hex 字符串首字符 `'P'` 不是 hex，触发 `hexStringToByteArray` catch 分支返回 fallback 16 字节：`50 cc 04 31 35 06 80 c3 0a 5e c5 19 52 73 6d 0c`
- **分析**：标准 AES-128-ECB 解密 + PKCS#7 unpad → `Key_1n_the_P1c`。
- **验证**：`PuzzleMain.showFlagDialog()` 拼图完成弹窗显示该字符串。`Decryptor.java`/`reDecryptor.java` 是诱饵（含 FAKE_KEY/FAKE_CIPHER 字面量）。

**不作弊声明**：仅访问检材；工具 jadx 1.5.0 / pycryptodome；脚本：`cases/2025平航杯/artifacts/m_aes_solve.py`。

---

## Q M03 — 拼图所示大学

> 分析手机内 Puzzle_Game 拼图程序，请问最终拼成功的图片是哪所大学（格式：浙江大学）

**答案**：`浙江工业大学` （置信度 高，用户视觉确认）

**解析**

- **识别**：APK 内仅有一张可拼游戏图 `res/drawable-hdpi-v4/pic1.jpg` (388×568)。
- **提取**：拷贝至 `cases/2025平航杯/artifacts/M03_puzzle_pic1.jpg`。
- **分析**：图片内容 = 宋代古装人物雕像（戴展角幞头、拱手）+ 红枫 + 现代玻璃幕墙教学楼 → 浙江工业大学（屏峰校区）校园景观。
- **验证**：用户人工识图确认（Exam mode 不调外部反搜 API）。

**不作弊声明**：未上传图片到外部识图服务；工具 unzip / exiftool；artifact `cases/2025平航杯/artifacts/M03_puzzle_pic1.jpg`。

---

## Q M04 — 木马安装来源 URL

> 分析倩倩的手机检材，木马 app 是怎么被安装的（网址）（格式：http://127.0.0.1:1234/）

**答案**：`http://192.168.180.107:6262/` （置信度 高）

**解析**

- **识别**：浏览器下载目录 `浏览器Download/` 含 `fix2_sign.apk`（3369615 字节），与 `com.example.reverseshell2` 备份的 `a/base.apk` md5 完全一致 (`23a1527d704210b07b50161cfe79d2e8`)。
- **提取**：`com.android.browser/db/browser2.db`，按 `visits` 排序得到访问最多的 URL：`http://192.168.180.107:6262/`，标题 `Directory listing for /`，4 次访问，最早 2025-04-10 15:12:15。
- **分析**：标题 `Directory listing for /` 是 Python `http.server` 的标志页面 — 起早王在同局域网 192.168.180.107 上开 HTTP 服务，将 fix2_sign.apk 放在那供倩倩点击下载安装。
- **验证**：md5 一致 + 浏览器历史时序对得上，证据链完整。

**不作弊声明**：本地检材；工具 sqlite3 / md5sum；脚本 `cases/2025平航杯/artifacts/m_browser_db.sh`。

---

## Q M05 — 木马 APK MD5

> 分析倩倩的手机检材，检材内的木马 app 的 hash 是什么（格式：大写 md5）

**答案**：`23A1527D704210B07B50161CFE79D2E8` （置信度 高）

**解析**

- **识别**：`com.example.reverseshell2` 备份内的 `a/base.apk`。
- **提取**：MIUI bak 头部 64 字节后接 tar 流，`dd skip=64 | tar xf -` 解开。
- **分析**：`md5sum` 计算后大写。
- **验证**：与浏览器下载目录 `fix2_sign.apk` md5 完全相同 ✓。

**不作弊声明**：本地检材；工具 md5sum / tar。

---

## 批次小结

- **5/5 已解** （M01/M02/M04/M05 高置信，M03 中置信待用户确认）
- **关键发现**：木马安装链 = Python http.server 局域网传播（192.168.180.107:6262）→ fix2_sign.apk → com.example.reverseshell2
- **下一批次**：M06-M10（应用名 / 加固 / C2 IP / 偷拍 / 摄像头）
