# 2025 平航杯 · C 计算机取证 · C11-C15（全部阻塞于 E 盘 BitLocker）

---

## 共同阻塞说明

C11-C16 全部依赖 **E 盘**（BitLocker 加密分区）的数据：
- `E:\facefusion_3.1.10\` — AI 换脸工作目录
- `E:\neo4j-community-3.5.14-windows\` — Neo4j 图数据库

**BitLocker 现状**：
- 恢复密钥 TXT（`121E1B22-A1A7-4A54-A8D4-799265C402CF.TXT`）被 `WYWZ/Eraser.dll` **彻底擦除**
- 扫 `$MFT` 366 MB、`pagefile.sys` 2.5 GB、整盘 80 GB 均未恢复 48 位密钥
- 无 `hiberfil.sys`（快速启动关），无法取 FVEK
- 无 `.BEK` 外部密钥文件

若用户场下能提供 BitLocker 密钥，可用：
```bash
sudo dislocker -r -V /dev/loop1 -p<密钥> -- /mnt/dis
sudo mount -o ro /mnt/dis/dislocker-file /mnt/win_e
```

---

## C11 — 被换脸图片数

> 起早王用 ai 换脸程序换了几张图片（格式：1）

**答案**：⛔ 未解（E 盘 BitLocker）

**方法（E 盘解锁后）**：统计 `facefusion/outputs/` 或 `output_path` 目录下生成图数。

---

## C12 — 最早换脸模型

> 最早换脸使用了哪个模型（格式：xxxxx_xxx.xxx）

**答案**：⛔ 未解

**方法**：查看 `facefusion.ini` 或 `~/facefusion/temp/` 日志，按 mtime 排序最早模型。

---

## C13 — Neo4j 数据库名

> Neo4j 中 x 的数据库名是什么（格式：xxx）

**答案**：⛔ 未解

**方法**：`E:\neo4j-community-3.5.14-windows\conf\neo4j.conf` 的 `dbms.active_database` 配置项，或 `data/databases/` 子目录名。

---

## C14 — Neo4j 节点数

> 相关数据库节点总数（格式：1）

**答案**：⛔ 未解

**方法**：Cypher `MATCH (n) RETURN count(n)`；或数 `data/databases/<db>/neostore.nodestore.db` 大小 ÷ 15 字节/node。

---

## C15 — 白杰手机号

> Neo4j 中白杰的手机号（格式：13111111111）

**答案**：⛔ 未解

**方法**：`MATCH (n {name:'白杰'}) RETURN n.phone`

---

**不作弊声明**
- C 盘已充分挖掘，E 盘密钥无法本地恢复
- 未访问外部网络获取密钥
- 工具列表同 C10
